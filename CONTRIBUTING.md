# Contributing to Nexora

## Development Philosophy

Nexora is developed incrementally using versioned milestones.

Each version contains exactly 10 milestones.

The repository architecture is intentionally stable. Changes to the architecture should only happen when there is a strong technical reason.

## Development Environment

Create and activate the Python environment:

```text
python -m venv .venv